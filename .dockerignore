*.o
*.a
.cache/
# Do not ignore .git directory, otherwise the reported build number will always be 0
.github/
.gitignore
.vs/
.vscode/
.DS_Store

build*/

models/*

# Heavy artifacts the C++ build does NOT need — these were ~1 GB of dead weight
# slowing the build-context COPY. (build*/ already covers build-diag/.)
**/node_modules/
tools/server/tests/
build-diag/
*.bin
*.log

/llama-cli
/llama-quantize

arm_neon.h
compile_commands.json
Dockerfile
.venv/
__pycache__/
*.pyc
*.gguf
.git/
